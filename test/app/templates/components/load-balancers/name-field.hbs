{{#if isEditMode}}
  {{float-label-input
    class="load-balancer-name"
    value=name
    required=true
    placeholder="Name"
    customValidation=validateName
    setErrorMessage=setNameErrorMessage
    maxlength="255"
  }}
{{else}}
  {{name}}
{{/if}}
